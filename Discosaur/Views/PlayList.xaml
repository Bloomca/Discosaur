<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Discosaur.Views.PlayList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Discosaur.Views"
    xmlns:models="using:Discosaur.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Click="AddFolder_Click" Margin="0,0,0,8">Open Folder</Button>

        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="AlbumsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:Album">
                        <local:AlbumView Album="{x:Bind}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
